export interface DashboardMetrics {
  occupancy: number;
  revenue: number;
  totalBookings: number;
}

export interface QuickNavItem {
  title: string;
  description: string;
  href: string;
  icon: string;
}
